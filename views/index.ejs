<!DOCTYPE html>
<html>
<head>
    <title>My NestJS App</title>
    <link href="/styles.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="max-w-3xl mx-auto p-4 bg-white shadow-md rounded-lg">
        <h1 class="text-2xl font-bold text-center mb-4"><%= title %></h1>
        <p class="text-gray-700 text-center mb-6"><%= message %></p>
        <ul>
          <% tasks.forEach(task => { %>
            <li class="p-2 border-b border-gray-300"><%= task.title %> - <%= task.description %> - <%= task.status %></li>
          <% }) %>
        </ul>
    </div>
</body>
</html>
